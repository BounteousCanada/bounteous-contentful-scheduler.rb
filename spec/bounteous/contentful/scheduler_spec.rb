RSpec.describe Bounteous::Contentful::Scheduler do
  it "has a version number" do
    expect(Bounteous::Contentful::Scheduler::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
